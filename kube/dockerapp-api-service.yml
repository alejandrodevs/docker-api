kind: Service
apiVersion: v1
metadata:
  name: dockerapp-api-service
spec:
  selector:
    app: dockerapp-api
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
